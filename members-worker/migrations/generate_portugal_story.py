import json

OUT_FILE = "017_portugal_story.sql"

def sql_escape(s):
    return s.replace("'", "''")

with open('/tmp/portugal_story.json', encoding='utf-8') as f:
    stories = json.load(f)

lines = []
lines.append("-- New story: Portugal's B2G SME/micro extension + PDF/QES deferral,")
lines.append("-- the first Portugal newsletter story, completing the end-to-end")
lines.append("-- new-country rollout per ADDING-A-COUNTRY.md's Phase 6 checklist.")
lines.append("-- Sourced to the official Portal das Financas e-Fatura portal.")
lines.append("-- Generated by migrations/generate_portugal_story.py.")
lines.append("")

for s in stories:
    lines.append(
        f"INSERT INTO stories (id, date, month, summary_en, html_en, source_url, published) VALUES "
        f"('{sql_escape(s['id'])}', '{s['date']}', '{s['month']}', "
        f"'{sql_escape(s['summary'])}', '{sql_escape(s['html'])}', '{sql_escape(s['source_url'])}', 1);"
    )
    for country in s["countries"]:
        lines.append(
            f"INSERT INTO story_countries (story_id, country_id) "
            f"SELECT '{sql_escape(s['id'])}', id FROM countries WHERE name_en = '{sql_escape(country)}';"
        )
    lines.append(
        f"INSERT INTO story_translations (story_id, lang, title, summary, html) VALUES "
        f"('{sql_escape(s['id'])}', 'en', '{sql_escape(s['title'])}', '{sql_escape(s['summary'])}', '{sql_escape(s['html'])}');"
    )

with open(OUT_FILE, "w", encoding="utf-8") as out:
    out.write("\n".join(lines) + "\n")

print(f"Written {len(stories)} story to {OUT_FILE}")
