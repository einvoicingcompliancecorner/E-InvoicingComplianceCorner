-- Botswana: country row + name translations. Generated by
-- new_country_scaffold.py; INSERT OR IGNORE throughout (idempotent).

INSERT OR IGNORE INTO countries (code, name_en, region, slug, in_picker) VALUES ('BW', 'Botswana', 'Middle East / Africa', 'botswana', 1);

INSERT OR IGNORE INTO country_translations (country_id, lang, display_name) SELECT id, 'en', 'Botswana' FROM countries WHERE code = 'BW';
INSERT OR IGNORE INTO country_translations (country_id, lang, display_name) SELECT id, 'es', 'Botsuana' FROM countries WHERE code = 'BW';
INSERT OR IGNORE INTO country_translations (country_id, lang, display_name) SELECT id, 'de', 'Botsuana' FROM countries WHERE code = 'BW';
INSERT OR IGNORE INTO country_translations (country_id, lang, display_name) SELECT id, 'fr', 'Botswana' FROM countries WHERE code = 'BW';

-- ---- what this migration claims it did (see apply_migrations.py) ----
-- INSERT OR IGNORE cannot fail; it can only decline. Assert the row
-- and all four display names are actually there, so a code that
-- collides with an existing country is caught in replay rather than
-- discovered as a missing menu entry.
--
-- ASSERT: SELECT count(*) FROM countries WHERE code = 'BW' = 1
-- ASSERT: SELECT in_picker FROM countries WHERE code = 'BW' = 1
-- ASSERT: SELECT count(*) FROM country_translations WHERE country_id = (SELECT id FROM countries WHERE code = 'BW') = 4
