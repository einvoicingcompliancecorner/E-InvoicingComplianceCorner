{
  "name": "einvoicing-compliance-corner",
  "version": "1.0.0",
  "private": true,
  "description": "The E-Invoicing Compliance Corner — test harnesses and the pinned deploy toolchain. The site itself is two Cloudflare Workers deployed with wrangler from members-worker/ and site-worker/. Run `npm install` once at the repo root: npx walks up the tree, so both Workers and apply_migrations.py then use the pinned wrangler below rather than downloading whatever is newest.",
  "type": "module",
  "scripts": {
    "test": "node tests/run-all.mjs",
    "test:roi": "node tests/roi-regression.mjs",
    "test:currency": "node tests/roi-currency.mjs",
    "test:contrast": "node tests/contrast-audit.mjs",
    "test:i18n": "node tests/roi-i18n.mjs",
    "contrast": "node tests/contrast-page.mjs",
    "count": "node tests/jurisdiction-count.mjs",
    "count:fix": "node tests/jurisdiction-count.mjs --fix",
    "test:migrations": "cd members-worker/migrations && python3 apply_migrations.py --replay-only && python3 test_assertions.py",
    "preview": "node tests/preview.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "wrangler": "4.122.0"
  }
}
